import AppBarAction from "./AppBarAction";
import AppBarActionsList from "./AppBarActionsList";
import AppBarRoot from "./AppBarRoot";

export const AppBar = {
  Root: AppBarRoot,
  Actions: AppBarActionsList,
  Action: AppBarAction,
};
